import ComingSoon from "../../components/ComingSoon";

const Prefetching = () => {
    return (
        <ComingSoon
            title="Prefetching & Preconnect"
            description="Prepare network connections early to reduce load delays. This feature is coming soon!"
        />
    );
};

export default Prefetching;
