import ComingSoon from "../../components/ComingSoon";

const RenderBlocking = () => {
    return (
        <ComingSoon
            title="Render Blocking Optimization"
            description="Avoid blocking the main thread by deferring scripts. This feature is coming soon!"
        />
    );
};

export default RenderBlocking;
