import ComingSoon from "../../components/ComingSoon";

const IdleCallback = () => {
    return (
        <ComingSoon
            title="requestIdleCallback"
            description="Execute low-priority tasks when the browser is idle. This feature is coming soon!"
        />
    );
};

export default IdleCallback;
