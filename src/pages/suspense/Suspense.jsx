import ComingSoon from "../../components/ComingSoon";

const Suspense = () => {
    return (
        <ComingSoon
            title="React Suspense & Concurrent"
            description="Enable smoother rendering using React's concurrent features. This feature is coming soon!"
        />
    );
};

export default Suspense;
